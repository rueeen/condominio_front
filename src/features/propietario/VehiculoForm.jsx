import { normalizarPatente } from '../../utils/patente'
export default function VehiculoForm({ form, onSubmit, disabled }) { const registration = form.register('patente'); return <form onSubmit={form.handleSubmit(onSubmit)} className="flex flex-col gap-3 sm:flex-row"><div className="grow"><input className="input h-14 w-full text-lg uppercase" placeholder="Ej.: AB1234 o ABC12345" {...registration} onChange={event => { event.target.value = normalizarPatente(event.target.value); registration.onChange(event) }}/>{form.formState.errors.patente && <p className="mt-1 text-sm text-red-600">{form.formState.errors.patente.message}</p>}</div><button disabled={disabled} className="btn-primary h-14 self-stretch disabled:opacity-50 sm:self-start">Solicitar</button></form> }
