import { Car } from 'lucide-react'
import CameraCapture from '../../components/CameraCapture'
import Spinner from '../../components/Spinner'
import { normalizarPatente } from '../../utils/patente'
import ResultadoAcceso from './ResultadoAcceso'
export default function VerificacionVehiculo({ access }) { return <section className="card min-h-[520px] space-y-5 p-6 sm:p-8" role="tabpanel"><div><h2 className="text-2xl font-bold">Verificar vehículo</h2><p className="mt-1 text-slate-500">Captura la patente o ingrésala manualmente.</p></div><CameraCapture onCapture={access.leerFoto} onRetry={access.limpiarLecturaCompleta}/><div><input className="input h-14 text-xl uppercase" aria-label="Patente del vehículo" placeholder="Ej.: AB1234 o ABC12345" value={access.patente} onChange={event => access.actualizarPatenteManual(normalizarPatente(event.target.value))}/>{access.confianza != null && <p className="mt-1 text-sm text-slate-500">Lectura automática{access.origen === 'ia' ? ' (asistida por IA)' : access.origen === 'tesseract' ? ' (Tesseract)' : ''}: {Math.round(access.confianza)}% de confianza{(access.confianza < 70 || access.origen === 'ia') && <span className="ml-1 text-amber-700">· Confirma la patente antes de continuar.</span>}</p>}{access.origen === 'ia' && <p className="mt-1 text-sm text-slate-500">Revisa especialmente los caracteres D/0, O/0 e I/1.</p>}</div><button disabled={access.loading} onClick={access.verificarPatente} className="btn-primary h-16 w-full text-xl disabled:opacity-50"><Car/> Confirmar patente</button>{access.loading ? <Spinner text="Procesando..."/> : <ResultadoAcceso {...(access.results.vehiculo || {})}/>}</section> }
