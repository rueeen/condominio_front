import { AlertTriangle, CheckCircle2, WifiOff, XCircle } from 'lucide-react'
export default function ResultadoAcceso({ status, title, details }) { if (!status) return null; const styles = { authorized: 'border-emerald-700 bg-emerald-600', rejected: 'border-red-700 bg-red-600', network: 'border-amber-700 bg-amber-600', server: 'border-orange-700 bg-orange-600' }; const Icon = status === 'authorized' ? CheckCircle2 : status === 'network' ? WifiOff : status === 'server' ? AlertTriangle : XCircle; return <div className={`mt-5 rounded-2xl border p-6 text-center text-white shadow-sm sm:p-8 ${styles[status]}`}><Icon className="mx-auto" size={64}/><h2 className="mt-3 text-3xl font-black sm:text-4xl">{title}</h2><p className="mt-2 text-xl sm:text-2xl">{details}</p></div> }
